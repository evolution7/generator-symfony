{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {<% if (bootstrap) { %>
    "sass-bootstrap": "~3.0.0",<% } %>
    "detectizr": "latest",
    "jquery": "~1.10.2"
  },
  "devDependencies": {}
}
